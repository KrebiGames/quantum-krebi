enum PlayerTeam {
	Blue, Red
}

component Player {
	FP WalkSpeed;
	FP NormalJump;

	[HideInInspector]
	PlayerRef PlayerRef;
}

component PlayerStatus {
	[ExcludeFromPrototype]
	player_ref PlayerRef;

	[ExcludeFromPrototype]
	entity_ref SpawnerEntityRef;

	[ExcludeFromPrototype]
	PlayerTeam PlayerTeam;

	asset_ref<PlayerMovementData> PlayerMovementData;
}

component PlayerSpawner {
	player_ref PlayerRef;
	PlayerTeam PlayerTeam;
}

component CrabOrientation {
	FP WorldYaw;
	FP LocalYaw;
	FP OrientationAngle;
	FP RotationSpeed;
}

component BodyAnchor {
}

component CrabClaw {
	FPVector3 RestLocalPosition;
	FPVector3 RestTwistAxis;
	FPVector3 RestSwingAxis;
}

signal OnPlayerRespawned(entity_ref playerEntityRef, bool fullReset);
signal OnPlayerRespawnTimerReset(entity_ref playerEntityRef);
